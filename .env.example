# OpenClaw Secrets (optional)
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# IMPORTANT: After filling in values, restrict permissions:
#   chmod 600 .env
#
# Empty values are skipped — the corresponding
# Key Vault secrets will not be created.

GITHUB_TOKEN=
ANTHROPIC_API_KEY=
